export enum AlertDirection {
  BOTTOM = 'bottom',
  TOP = 'top',
}

export enum AlertAlign {
  RIGHT = 'right',
  LEFT = 'left',
  CENTER = 'center',
}

export enum AlertType {
  INFO = 'info',
  SUCCESS = 'success',
  ERROR = 'danger',
  WARNING = 'warning',
  PRIMARY = 'primary',
}
